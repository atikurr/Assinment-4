1. Difference between getElementById, getElementsByClassName, querySelector, querySelectorAll

answer:
getElementById: = one element,Returns only one element
getElementsByClassName = Selects elements with the same class name,
querySelector =Uses CSS selector , Returns the first matching element
querySelectorAll = Uses CSS selector, Returns all matching elements

2)How to create and insert a new element:

Answer: 
We use createElement() to create a new element.

3)What is Event Bubbling?
Answer:
Event bubbling means the event goes from child to parent and the event moves upward

4)What is Event Delegation? Why is it useful?

Answer:
Event delegation means adding event to a parent instead of many children.

useful:
Less code , Better performance , Works for new elements

5)Difference between preventDefault() and stopPropagation()

Answer:

preventDefault() = stop default behavior
stopPropagation() = stop event flow
